git config user.name "Deeban Chakravarthy"

git config user.email "vdeeban@gmail.com"

Branch

git branch new

git checkout new

git branch

Committing

git commit -a

merge git merge main

git merge --squash

pull

git pull

git pull --quiet

git pull --squash

git pull --no-commit

git pull --no-ff

rebase

git rebase new

git rebase --continue

git rebase --abort

status

git status

add

git add hello.txt

init

git init hello1

clone

git clone https://github.com/vdeeban/Infrastructure-as-Code-Using-Terraform-Modules-.git

deleting branch

git branch -d 2(branch name)

branch merge

git merge main
